{
  "name": "skiller-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset",
    "release": "bash scripts/release.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.31.1"
  }
}
